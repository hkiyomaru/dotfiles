# ------------------------------------------
# Misc
# ------------------------------------------
# fzf
[ -f $HOME/.fzf.zsh ] && source $HOME/.fzf.zsh

# anyenv
[ -d "${HOME}/.anyenv" ] && eval "$(anyenv init -)"
